logging:
    file: target/${spring.application.name}.log
    level:
        ROOT: INFO
        org.ylzl.eden: DEBUG

spring:
    application:
        name: #project.name#
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        type: com.zaxxer.hikari.HikariDataSource
        url: jdbc:mysql://localhost:3306/#project.name#?useUnicode=true&characterEncoding=utf8&useSSL=false&rewriteBatchedStatements=true
        username: root
        password: DevOps@303
        hikari:
            data-source-properties:
                cachePrepStmts: true
                prepStmtCacheSize: 250
                prepStmtCacheSqlLimit: 2048
                useServerPrepStmts: true
    jmx:
        default-domain: ${spring.application.name}
    jpa:
        database: #spring.jpa.database#
        database-platform: #spring.jpa.database-platform#
        hibernate:
            ddl-auto: none
        naming:
            implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
            physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
        open-in-view: false
        properties:
            hibernate:
                cache:
                    use_second_level_cache: false
                    use_query_cache: false
                    use_minimal_puts: true
                    hazelcast.instance_name: ${spring.application.name}
                    hazelcast.use_lite_member: true
                ejb:
                    entitymanager_factory_name: ${spring.application.name}
                generate_statistics: true
                id:
                    new_generator_mappings: true
                region:
                    factory_class: com.hazelcast.hibernate.HazelcastCacheRegionFactory
                show-sql: true
    kafka:
        bootstrap-servers: 127.0.0.1:9092 # 多个用英文逗号隔开
        producer:
            batch-size: 100 # 默认为 0，批处理发送主题大小
            retries: 3 # 默认为 0，发送主题失败后重试的次数
        consumer:
            session-timeout: 10000 # 会话超时（毫秒）
            heartbeart-interval: 5000 # 心跳频率（毫秒），配置值不能超过会话超时
            max-poll-records: 100 # 单次拉取最大记录数
            group-id: sample # 消费组，消费者多实例的情况下，配置同一个消费组，实例数不能超过 Kafka 的分区数
            enable-auto-commit: false # 建议关闭自动提交 Offset，不然报错很难处理
            auto-offset-reset: earliest # 如果 Offset 找不到时重置的规则
        listener:
            concurrency: 5 # 消费监听线程数，当配置值大于 Kafka 分区数，按分区数执行
            poll-timeout: 5000 # 单次拉取消息的超时（毫秒）
            ack-mde: MANUAL_IMMEDIATE # 手动提交模式
        template:
            default-topic: sample_users
    mvc:
        favicon:
            enabled: false
    profiles:
        active: #spring.profiles.active#
        include: #spring.profiles.include#
